# 별샘 리뉴얼 실행 계획서

> 작성일 2026-08-10
> 개정 이력: ① 사주 제외, 점성술 전문화 확정 ② **DB 우선 아키텍처 확정 (LLM 옵트인 격리)**
> 목표: Awwwards급 비주얼 + 점성술 단일 도메인 압도적 품질
> 이 문서만 보고 바로 착수 가능하도록 작성됨. HANDOFF.md(사업 로드맵)와 함께 사용.

---

## 0. 제품 전략

### 0.1 사주는 하지 않는다

한국 최대 검색 수요를 포기하는 대신 점성술 하나를 경쟁자가 따라올 수 없는 깊이로 판다.
사주 시장은 전문가 상담까지 붙은 대형사 판이고, 어설픈 사주는 정통성 의심만 산다.

### 0.2 ⭐ 핵심 아키텍처 원칙 — DB 우선, LLM 옵트인

> **LLM 비용은 트래픽이 아니라 매출에 비례해야 한다.**

별샘은 광고 수익 모델이다. 조회 1회당 광고 수익이 1~2원인데 조회마다 LLM을 호출하면
**트래픽이 늘수록 적자**가 된다. 실제 업계도 두 진영으로 갈려 있고, 광고 모델은 전자다.

| 진영 | 생성 방식 | 수익 모델 | 예 |
| --- | --- | --- | --- |
| **콘텐츠형** ← 별샘 | 계산 + 사전 작성 텍스트 조합 | 광고 / 라이트 프리미엄 | 포스텔러, 점신 오늘의 운세 |
| AI 대화형 | LLM 실시간 | 구독 | AI 점성술 챗 앱 |

**그래서 이렇게 간다:**

```
[결과 화면]
├─ 즉시 표시 — 선택지 기반 DB 조립 리포트     ← 기본 경로. LLM 0회
└─ [🔮 AI로 더 깊이 보기] 버튼                ← 클릭한 사람만 LLM 1회
```

LLM 비용이 트래픽이 아니라 **버튼 클릭 수**에 비례한다. 나중에 유료화 지점으로도 최적.

**개인화 ≠ LLM.** 개인화는 차트 계산에서 나오고, 텍스트는 유한한 아톰 DB에서 조립된다.
포스텔러가 개인 맞춤 운세를 LLM 없이 주는 방식이 정확히 이것.

### 0.3 입력 방식 — 자유 입력 → 선택지

기존: 고민을 자유 텍스트로 입력. 신규: **관심사 선택지 탭 1회.**

- 모바일에서 자유 입력은 마찰. 선택지가 전환율이 높다
- 선택지는 유한 → DB 조회 가능 → LLM 불필요
- 선택 행위 자체를 연출로 만들 수 있다 (원형 별자리 선택, 관심사 카드 뒤집기)
- 자유 질문은 폐기하지 않고 "AI로 더 깊이 보기" 안으로 이전 (강점 보존)

### 0.4 압도적 품질의 정의

1. **실제 천문 계산 기반** — 태양별자리 12분류가 아니라 출생 순간의 전체 하늘
2. **어스펙트(각도) 반영** — 현재 최대 약점(§5.1). DB 조립의 전제 조건이기도 함
3. **근거 명시 서술** — "화성 ☌ 태양 (오브 2.1°)" 를 UI에 칩으로 노출 → 계산했다는 시각적 증명
4. **매일 바뀌는 진짜 하늘** — 무작위 뽑기가 아닌 실제 트랜짓
5. **즉시 응답** — 대기 0초. 로딩을 가릴 연출이 필요 없어져 디자인 자유도가 커짐
6. **Awwwards급 시각화**

### 0.5 디자인 읽기 (Design Read)

**"한국 모바일 사용자 대상 프리미엄 점성술 서비스. 다크 코스믹 비주얼 언어,
Awwwards-실험적 성향. 단 검색 유입이 생명이므로 화려함은 메인·결과 페이지에 집중,
콘텐츠·SEO 페이지는 가볍게."**

| 다이얼 | 값 | 근거 |
| --- | --- | --- |
| DESIGN_VARIANCE | 8 | Awwwards 지향, 비대칭 레이아웃 |
| MOTION_INTENSITY | 8 | 스크롤 연출 + WebGL. 블로그·SEO 페이지는 3으로 강등 |
| VISUAL_DENSITY | 3 | 여백 중심, 프리미엄 |

---

## 1. 기술 스택 (2026-08-10 최신 안정 버전 확인 완료)

| 기술 | 버전 | 역할 |
| --- | --- | --- |
| Node.js | 24 LTS (로컬 v24.13.1 확인) | 런타임 |
| **Next.js** | **16.3.x** (2026-08-03 릴리스) | 프레임워크. **`output: 'export'` 정적 빌드** |
| React | 19.2.x | UI |
| TypeScript | 5.x (Next 동봉) | 타입 |
| **Tailwind CSS** | **4.3.x** | 스타일. v4 방식: `@tailwindcss/postcss`, CSS-first 설정 |
| **GSAP** | **3.14** | 스크롤 연출 (ScrollTrigger·SplitText 등 **전 플러그인 무료**) |
| **Three.js** | **r184** | WebGL 별하늘 |
| **@react-three/fiber** | **9.7.x** | Three.js의 React 바인딩 (React 19 호환) |
| @react-three/drei | 최신 (fiber v9 호환) | R3F 유틸 |
| **Lenis** | **1.3.x** | 부드러운 스크롤 |
| **Motion** (`motion/react`) | 최신 | UI 마이크로 모션. GSAP과 같은 트리에 섞지 않기 |
| @phosphor-icons/react | 최신 | 아이콘 (한 패밀리, strokeWidth 1.5 통일) |
| **kerykeion** (백엔드) | 5.x | 점성술 엔진 — 네이탈·시너스트리·트랜짓·컴포짓·리턴·어스펙트(11종)·달위상 |

### 설치

```bash
npx create-next-app@latest byeolsaem-web --typescript --tailwind --app --src-dir
cd byeolsaem-web
npm i three @react-three/fiber @react-three/drei gsap lenis motion @phosphor-icons/react
npm i -D @types/three
```

`next.config.ts`:

```ts
const nextConfig = {
  output: 'export',              // 정적 빌드 → Cloudflare 정적 호스팅 그대로
  images: { unoptimized: true }, // export 모드 필수
  trailingSlash: false,
};
```

### 아키텍처 원칙

- **Server Component 기본, 모션·WebGL은 `'use client'` 잎(leaf) 컴포넌트로 격리**
- GSAP과 Motion 역할 분담: GSAP+ScrollTrigger = 페이지 스크롤텔링(핀·스크럽),
  Motion = 컴포넌트 단위(버튼 hover, 카드 등장, 전환). 같은 트리에서 혼용 금지
- `window.addEventListener('scroll')` 금지. `useScroll()` / ScrollTrigger / IntersectionObserver만
- 연속값(마우스·스크롤)은 `useMotionValue`/`useTransform`. `useState` 금지
- 배포: GitHub push → Cloudflare 자동 배포. wrangler.jsonc `assets.directory`를 `out/`으로 변경

---

## 2. ⭐ 해석 DB (아톰 DB) — 이 프로젝트의 핵심 자산

LLM은 매번 사고 버리는 비용이지만, DB는 **한 번 만들면 남는 자산**이다.
크롤러가 읽을 수 있어 SEO에도 직접 기여한다 (LLM 생성 결과는 구글이 못 본다).

### 2.1 규모 — 유한하다

| 아톰 종류 | 개수 | 용도 |
| --- | --- | --- |
| 행성 × 별자리 | 10 × 12 = 120 | 네이탈 기본 |
| 행성 × 하우스 | 10 × 12 = 120 | 네이탈 기본 |
| 어스펙트 (45쌍 × 주요 5각도) | 225 | 네이탈 핵심 해석 |
| 상승궁 | 12 | 네이탈 |
| **소계 (네이탈)** | **약 480** | |
| 트랜짓 행성 10 × 각도 5 × 네이탈 행성 10 | 500 | 오늘의 운세 개인화 |
| 관심사 렌즈 (7 관심사 × 관련 배치) | 약 1,000 | 선택지 대응 |
| **총계** | **약 2,000** | |

2,000개 × 300자 ≈ 60만자. **진짜 비용은 API가 아니라 제작 노동.**
Gemini로 초안 배치 생성 → 사람 검수 → JSON 커밋. 몇 주 작업이지만 일회성 자산.

### 2.2 저장 위치

| 데이터 | 위치 | 이유 |
| --- | --- | --- |
| **해석 아톰 (읽기 전용)** | 저장소에 JSON. 프론트 번들에 포함 | 버전 관리, 인프라 0, 즉시 조회 |
| **결과 캐시 (쓰기)** | **Cloudflare KV** | **Render 무료 티어는 파일시스템이 휘발성.** SQLite 두면 재시작 시 소멸 |
| 일일 운세 생성물 | Cloudflare KV | cron 생성 → 정적 서빙 |

```
data/
├─ placements/   행성×별자리, 행성×하우스
├─ aspects/      어스펙트 225
├─ transits/     트랜짓 조합 500
├─ concerns/     관심사 렌즈
└─ signs/        12별자리 기본
```

### 2.3 조립 로직 (결정론)

1. kerykeion으로 차트 계산 → 배치·어스펙트 목록 산출
2. 선택된 관심사에 해당하는 팩터만 필터 (예: 재물운 → 2·8하우스, 목성, 금성 + 관련 어스펙트)
3. **강도순 정렬** — 오브(orb)가 작을수록 강하게 작용. 상위 N개만 채택
4. 점수 계산도 규칙 기반: 트라인·섹스타일 가산, 스퀘어·오포지션 감산, 오브 가중
5. 템플릿에 배치 → 즉시 렌더

**같은 입력이면 항상 같은 출력.** 점술 서비스의 신뢰 조건.

### 2.4 🚨 애드센스 경고 — 자동 대량 페이지 생성 금지

DB가 생기면 "조합마다 페이지 하나씩 자동 생성"이 유혹적이지만 **하지 않는다.**
얇은 페이지 수천 개 양산은 구글 스팸 정책(scaled content abuse) 직격이고,
이전 애드센스 거절 이력을 고려하면 치명적이다.

**발행 원칙: 선별해서 두껍게.** 예 — 12별자리 × 주요 5관심사 = 60페이지, 각 2,000자 이상.
나머지 아톰은 앱 내부 조합용으로만 쓰고 페이지로 발행하지 않는다.

---

## 3. 사이트 구조

```
byeolsaem.com
├─ /                     메인 허브 — WebGL 하늘 + 오늘의 하늘 + 도구 입구
├─ /natal                출생 천궁도 분석 (기존 기능 이식·강화) ★ 주력
├─ /today                오늘의 운세 — 실제 트랜짓, 매일 갱신 ★ 재방문 엔진
├─ /sign/[별자리]         12별자리 페이지 (입력 불필요, 정적) ★ SEO 1층
├─ /synastry             궁합 — 두 사람 차트 비교 ★ 바이럴
├─ /yearly/2027          연간 운세 (신년 시즌 주력)
├─ /retrograde           수성 역행 캘린더 (자동 계산) — SEO 자석
├─ /moon                 달 위상 (선택)
├─ /blog/[슬러그]         MDX 콘텐츠 (한글 키워드 슬러그)
└─ /about, /privacy
```

한국어 단일 우선. 기존 "한 페이지에 한/영 동시 렌더링" 구조는 폐기. `/en`은 후순위.

**기존 URL 301 매핑 (Cloudflare Redirect Rules):**
`/` 유지, `/blog-list→/blog`, `/blog.html→/blog/<슬러그>`, `/about`·`/privacy` 유지.
Search Console 사이트맵 재제출까지가 이전 완료 조건.

---

## 4. 디자인 시스템

### 4.1 컬러 — "먹빛 하늘에 금빛 별" (다크 단일 테마 고정)

밤하늘 주제이므로 다크 고정이 정당함. 섹션 간 라이트 반전 금지.
AI-보라색 글로우 금지. 액센트는 금빛 하나로 고정.

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--ink` | `#0B0E1A` | 배경 (순검정 금지, 남색 기운 먹색) |
| `--ink-raised` | `#131629` | 카드·패널 |
| `--starlight` | `#E8E4D8` | 본문 텍스트 (순백 금지, 별빛 미색) |
| `--starlight-dim` | `#9A96A8` | 보조 텍스트 |
| `--gold` | `#C9A227` | 유일한 액센트 — CTA, 별자리 선, 강조 |
| `--gold-soft` | `#E3C568` | 액센트 hover / 밝은 단계 |

두 번째 액센트 색 추가 금지. 행성·어스펙트 구분은 금빛의 명도 단계로 처리.

### 4.2 타이포그래피

| 역할 | 서체 | 근거 |
| --- | --- | --- |
| 디스플레이 | **마루 부리** (네이버, 무료, woff2 셀프호스팅) | 별·기록물 장르에 세리프가 정당. "천문 기록"의 격 |
| 본문/UI | **Pretendard Variable** | 한글 가독성 표준. `next/font/local` |
| 숫자·좌표 | Pretendard + `tabular-nums` | 별도 모노 서체 불필요 |

- `next/font/local` + `font-display: swap`. Google Fonts `<link>` 금지
- 제목: `text-4xl md:text-6xl tracking-tight`, 히어로 헤드라인 최대 2줄
- 강조는 같은 서체의 굵기·이탤릭으로

### 4.3 시그니처 — "오늘의 하늘"

**이 사이트가 기억될 한 가지:** 타로가 무작위로 카드를 뽑는 자리를 **실제 오늘 하늘**이 대신한다.
매일 실제 달의 별자리와 주요 각도를 WebGL 하늘에 그대로 렌더하고, 그것이 곧 "오늘의 카드"다.
무작위가 아니라 천문 데이터라는 점이 정통성이자 차별점.

**보조 연출:** 생년월일시 입력 → 배경 별들이 모여 사용자의 천궁도로 **선을 그리며 조립**되는 전환.
DB 조립은 즉시 응답이므로 이 연출은 **로딩 가리개가 아니라 순수한 연출**이다 (원하는 길이로 설계 가능).

화려함 예산은 이 둘에 집중. 나머지 섹션은 절제.

### 4.4 모션 규칙

- 히어로 WebGL 별하늘 (마우스 시차) — 저사양 폴백 필수 (§7)
- Lenis 전역 + GSAP ScrollTrigger 핀 섹션은 메인 1곳, 결과 1곳만
- 결과 공개: 천궁도 SVG 선 드로잉 (`stroke-dashoffset`), 점수 카운트업, 섹션 스태거
- 카드·버튼: Motion spring (`stiffness: 100, damping: 20`), `:active`에 `scale-[0.98]`
- **`prefers-reduced-motion` 전면 대응**: WebGL 정지, 스크롤 연출 즉시 표시
- 마퀴 최대 1개/페이지. 커스텀 커서 금지. "Scroll ↓" 유도 문구 금지

---

## 5. 페이지별 설계

### 5.1 메인 `/`

1. **히어로** (100dvh, 비대칭): 좌하단 헤드라인 "당신의 하늘을 읽어드립니다" +
   주 CTA 1개(내 천궁도) + 보조 1개(오늘의 하늘). 배경 WebGL 별하늘. 텍스트 요소 최대 4개
2. **오늘의 하늘 티저**: 오늘 날짜 + 달의 별자리 + 12별자리 선택 스크롤스냅 → `/today`
3. **도구 벤토** (비대칭 3셀: 천궁도 / 궁합 / 연간운세 — 각 셀 실제 비주얼)
4. **결과물 미리보기**: 실제 결과 화면 캡처 1장 (가짜 div 목업 금지)
5. 푸터: 연락처, 정책, 쿠팡 고지

### 5.2 출생 천궁도 `/natal` — 주력

- 입력: 생년월일시 + 도시(한글 자동완성, 기존 로직 재사용) + **관심사 선택지 탭 1회**
  - **"시간 모름" 옵션 필수** — 시간 없으면 하우스·상승궁 계산 불가 → 태양·달 중심 축약 리포트로 분기
- 계산: kerykeion — 행성 위치 + **어스펙트** + 앵글(ASC/MC) + 노드 (§6.1)
- **기본 리포트: DB 조립, 즉시 표시, LLM 0회**
  - 종합 점수(규칙 계산) + 관심사별 해석 + 강도 상위 어스펙트 + 근거 칩
- **[🔮 AI로 더 깊이 보기]**: 클릭 시에만 LLM 1회. 여기서 자유 질문 입력 허용
- 표시: 천궁도 SVG 선 드로잉. 기존 시각화 품질 유지하며 이식
- 공유: 카카오 공유 + 결과 이미지 저장 (기존 강점 그대로 이식)
- 하단: "천궁도란", "상승궁이란" SEO 텍스트

### 5.3 오늘의 운세 `/today` — 재방문 엔진

- 원리: 오늘 실제 행성 위치 vs 사용자 출생 차트 각도 (kerykeion 트랜짓)
- **2단 구성 — 둘 다 LLM 0회:**
  - **12별자리 공통**: 정적/cron. 로그인·입력 불필요
  - **개인 트랜짓**: 500개 트랜짓 아톰 DB 조립. **개인화되지만 LLM 0회, 즉시 표시**
- 무료·매일·대량 지면이므로 여기에 LLM을 붙이면 재방문이 늘수록 적자 (§0.2)
- 공유: "오늘의 한 줄" 이미지 저장 (기존 저장 로직 재사용)

### 5.4 12별자리 `/sign/[별자리]` — SEO 1층

- 12개 정적 페이지. 입력·로그인 불필요, 서버 호출 0
- 내용: 별자리 기본 해석 + 이번 달 트랜짓 + 2027 연간 요약 + `/natal` 유도 CTA
- 관심사별 확장 시 12 × 5 = 60페이지까지. **각 2,000자 이상** (§2.4)
- **11월까지 발행 완료** — 12월에 올리면 색인 시간이 부족해 신년 트래픽을 못 받는다
- 모션 3단계, WebGL 없음. 속도 우선

### 5.5 궁합 `/synastry` — 바이럴

- 입력: 두 사람 생년월일시
- 계산: kerykeion 시너스트리 (양방향 하우스 오버레이 + Ciro Discepolo 관계 점수 지원)
- 기본: DB 조립 (시너스트리 어스펙트 아톰). 심층만 LLM 옵트인
- 공유 카드 최적화 — 커플 콘텐츠는 공유율이 높다. OG 이미지 동적 생성 검토

### 5.6 연간 운세 `/yearly/2027` — 신년 시즌 주력

- 원리: 목성(별자리당 약 1년)·토성 이동이 개인 차트의 어느 하우스를 지나는가
  - 참고: 2027년 목성 사자자리 → 7/26 처녀자리 진입, 토성 양자리 체류
- 3층: ① 12별자리 공통 요약(= `/sign`에 포함) ② 개인 차트 연간 리포트(DB 조립)
  ③ **중요 날짜 캘린더** — 정확히 각도 맺는 날짜 목록. 순수 계산, 베끼기 어려움. 프리미엄 후보
- 별도 상품: **솔라 리턴**(생일 기준 1년) — 생일마다 재방문 트리거. 10월 이후

### 5.7 수성 역행 `/retrograde` — SEO 자석

- 연 3~4회 검색 폭발 키워드. 입력 불필요, 전부 자동 계산
- 빌드 시 향후 2년 역행 구간 계산 → 정적 페이지. 진행 중이면 D-day 표시
- 유지비 0, 상시 유입

### 5.8 블로그 `/blog/[슬러그]`

- MDX + 한글 키워드 슬러그 (`/blog/수성역행-뜻`)
- 기존 5편 이식. 신규 글은 md 파일 추가만으로 발행
- 모션 최소, WebGL 없음. 메타·OG·사이트맵 자동 생성

---

## 6. 백엔드 계획 (FastAPI + Python + Render 유지)

스택 유지 근거는 HANDOFF.md 참조. 외부 유료 점성술 API는 요청당 과금이라
트래픽 성장이 곧 비용이 되므로 채택하지 않는다.

**DB 우선 전환 후 백엔드 역할이 가벼워진다** — 대부분의 요청은 "차트 계산"만 하고
텍스트 조립은 프론트가 번들된 JSON으로 처리 가능. LLM 호출은 옵트인 버튼에만.

### 6.1 ⭐ 차트 데이터 확장 — 최우선 선행 과제

**현재 최대 약점:** `logic.py`가 산출하는 것은 행성 8개의 별자리·하우스뿐.
**어스펙트(행성 간 각도)가 통째로 빠져 있다.** 어스펙트는 점성술 해석의 핵심이자
**DB 조립의 전제 조건**이다 (아톰 225개가 어스펙트 기반).

kerykeion이 이미 11종 어스펙트를 오브 설정과 함께 계산한다. 추가할 것:

- **어스펙트 전체** (컨정션·트라인·스퀘어·오포지션·섹스타일 + 오브 값)
- **앵글**: ASC(상승궁)·MC(중천) — 상승궁은 쓰고 있으나 MC 미사용
- **노드**: 노스노드/사우스노드
- **역행 여부**: 각 행성 retrograde 플래그
- **달 위상**: kerykeion 지원

**리뉴얼 착수 전에 먼저 해도 되는 작업. DB 제작보다 앞서야 한다.**

### 6.2 엔드포인트 설계

| 경로 | 용도 | LLM |
| --- | --- | --- |
| `POST /chart` | 차트 계산만 반환 (배치·어스펙트 JSON) | 0 |
| `POST /chart/synastry` | 두 차트 비교 계산 | 0 |
| `POST /chart/transit` | 트랜짓 계산 | 0 |
| `POST /deep` | **AI 심층 해석 (옵트인 버튼 전용)** | 1회 |
| `GET /daily` | 12별자리 공통 (cron 생성용) | 하루 12회 |

정적 계산(수성 역행, 달 위상)은 빌드 타임 처리 — 런타임 호출 불필요.

### 6.3 프롬프트 구조 개편 (LLM 잔존 경로용)

현재 페르소나·출력 포맷·파싱이 한 f-string에 엉켜 있다. 3층 분리:

| 층 | 내용 | 관리 |
| --- | --- | --- |
| 페르소나 | '천체 데이터 분석가' 톤, 근거 중심 원칙 (**기존 강점, 유지**) | 공통 1곳 |
| 스키마 | 출력 구조 (Gemini `response_schema` JSON 강제) | 기능별 |
| 렌더링 | 레이아웃·별점·애니메이션 | **프론트** |

- **AI가 마크다운 레이아웃을 그리지 않는다.** 점수는 정수(0~100)로 받아 프론트가 별점 변환.
  문자열 `★★★☆☆`로 받으면 카운트업·스태거 연출을 못 한다
- `basis`(근거) 필드를 스키마에 강제 → 근거 누락 불가. UI에 칩으로 노출
- regex 파싱(`### Category:`, `[[아이템]]`) 전면 제거
- 한/영 중복 통짜 프롬프트 2벌 → 스키마는 언어 중립, 페르소나 텍스트만 언어별

### 6.4 🚨 연도 하드코딩 — 즉시 수정 필요

`logic.py`에 `2026년 인생 지표`, 폴백 키워드 `"2026운세"`가 박혀 있다.
**신년 시즌에 정확히 터진다.** 1월 접속자가 "2026년 지표"를 받게 됨.
`datetime.now().year` 또는 요청 파라미터로 변수화할 것. 리뉴얼과 무관하게 지금 고친다.

### 6.5 개선 항목 착수 순서

1. **연도 변수화** (§6.4) — 즉시, 10분
2. **1차 묶음**: async 블로킹 수정 · 에러 응답 정리 · rate limit(봇 방어) · 지오코딩 로컬화
3. **차트 데이터 확장** (§6.1) — DB 제작의 전제
4. **결과 캐시** — `hash(입력)` → Cloudflare KV. 일관성 + 비용 + 속도
5. 구조화 출력 (§6.3) — 옵트인 LLM 경로에 적용
6. SSE 스트리밍 — 옵트인 LLM 경로에만 (기본 경로는 즉시 응답이라 불필요)
7. 일일 운세 cron: Cloudflare Worker cron → Render → KV
   (기존 `byeolsaem-keepalive` Worker에 트리거 추가)
8. 모델 고정: `gemini-3-flash-preview` → GA 출시 확인 후 고정

---

## 7. 성능·품질 가드레일

Awwwards 비주얼과 검색 유입을 동시에 잡는 조건. 타협 불가.

- **LCP < 2.5s**: WebGL은 히어로 텍스트 페인트 후 지연 로드.
  첫 페인트는 CSS 그라디언트 하늘 → 캔버스 페이드인
- **WebGL 폴백 3단계**: ① 데스크톱 풀 씬 ② 모바일 파티클 축소 + DPR 1.5 캡
  ③ `prefers-reduced-motion` / WebGL 미지원 → 정적 별하늘 이미지
- Three.js 번들은 메인·결과 페이지만 dynamic import.
  **`/sign`·`/retrograde`·`/blog`에 실리지 않게 라우트 단위 분리**
- **아톰 DB JSON도 라우트별 분할 로드** — 2,000개를 한 번에 번들에 넣지 않는다.
  관심사·기능 단위로 쪼개 필요한 것만 import
- CLS < 0.1: 폰트 `size-adjust` 폴백, 이미지 width/height 명시
- 모바일 `< 768px`: 비대칭 레이아웃 1열 붕괴 명시
- WCAG AA: 금빛(`#C9A227`) 위에는 먹색 글자. 흰 글자 금지
- 접근성: 키보드 포커스 가시화, 결과는 실제 DOM 텍스트 (캔버스 텍스트 렌더 금지)

---

## 8. 일정

| 시기 | 작업 | 완료 기준 |
| --- | --- | --- |
| **8월 잔여** | 연도 하드코딩 수정 · 백엔드 1차 묶음 · **차트 데이터 확장(§6.1)** · Awwwards 레퍼런스 3~5개 → 무드보드 확정 | 어스펙트 산출 확인 + 디자인 컨셉 1장 |
| **8월 말~9월** | **아톰 DB 제작 착수** (Gemini 배치 생성 → 검수 → JSON). 우선순위: 네이탈 480 → 관심사 렌즈 → 트랜짓 500 | 네이탈 아톰 완료 |
| **9월 1주** | Next 16.3 뼈대, 디자인 토큰, WebGL 히어로, 메인 | 메인 데스크톱+모바일 완성 |
| **9월 2주** | `/natal` (DB 조립 + 옵트인 LLM), `/today` 2단 구성 | 두 도구 실사용 가능 |
| **9월 3주** | `/sign` 12페이지, 블로그 MDX 이식, 301 리다이렉트, 성능 최적화, 배포 전환 | Lighthouse 90+, Search Console 재제출 |
| **10월** | `/synastry`, `/yearly/2027`, `/retrograde`, 모션 폴리시, 애드센스 재신청 | 신년 시즌 상품 준비 완료 |
| **11월** | `/sign` + `/yearly` **색인 완료 확인**. 이후 **동결** | Search Console 색인 확인 |
| **12~2월** | 신년 시즌 대응. 설날엔 `/synastry` 푸시 | HANDOFF 판단 게이트 |

**두 개의 제약 조건:**
1. **색인 리드타임** — 12월에 페이지를 올리면 신년 트래픽을 못 받는다. 11월 마감
2. **아톰 DB 제작 노동** — 몇 주 걸린다. 프론트 개발과 **병렬로** 진행해야 9월 안에 끝난다

---

## 9. 착수 시 첫 명령

```bash
npx create-next-app@latest byeolsaem-web --typescript --tailwind --app --src-dir
```

기존 사이트는 리뉴얼 완료까지 그대로 서빙 유지.
별도 폴더(`byeolsaem-web/`)에서 개발 → 검증 후 wrangler 자산 경로만 교체하는 무중단 전환.
진행 순서: 브레인스토밍(무드보드) → 계획 검토 → 구현.

---

## 10. 시각 리뉴얼 — 배경·셰이더 (별도 서브프로젝트, 페이지 완성 후 착수)

2026-08-12 결정. 페이지를 먼저 채우고, 시각 품질은 통째로 한 번에 올린다.

### 10.1 왜 미뤘나

배경을 구간마다 바꾸는 시안(색 여정 / 장면 앵커 / 광원 이동, 그리고 SVG 장면
그림 3종)을 만들어 검토한 결과, **SVG 선 그림을 얹는 접근 자체가 틀렸다**는
결론에 도달했다. 평면 색 위의 균일한 실선은 어떤 형태를 그려도 도면처럼 보인다.
실제 수상작의 배경은 형태가 아니라 렌더링에서 갈린다:

| 싸 보이는 것 | 비싸 보이는 것 |
| --- | --- |
| 균일한 굵기의 실선 | 거리에 따른 밝기 감쇠 |
| 단색 채움 | 노이즈가 섞인 밀도 |
| 선명한 경계 | 블룸으로 번지는 광원 |
| 매끈한 그라디언트 | 미세한 필름 그레인 |
| 정지 | 아주 느린 표류(drift) |

같은 황도 12궁 링이라도 SVG 실선이면 도면이고, 셰이더로 그려 블룸을 먹이면
실제로 떠 있는 고리가 된다. 우리는 이미 Three.js와 커스텀 셰이더로 별하늘을
돌리고 있으므로, **덮는 것이 아니라 그 셰이더를 확장**하는 것이 맞다.

현재 적용되어 있는 것: 스크롤 구간별 그라디언트 교차(밤의 여정, §NightJourney).
이 서브프로젝트에서 대체되거나 셰이더 안으로 흡수될 수 있다.

### 10.2 범위 — 배경만이 아니라 전체 시각 언어

착수 시점에 **지금까지 만든 화면 전체를 셰이더 기반으로 올릴 수 있는지, 그것이
나은지**부터 판단한다. 배경만 고급스러워지고 나머지가 그대로면 이질감만 커진다.

검토 대상:
- 배경 — 3D 노이즈 성운, 스크롤로 움직이는 유니폼, 카메라 표류, 구간별 별 밀도
- 포스트프로세싱 — 블룸 · 필름 그레인 · 비네트 · 색수차 (**비용 대비 체감 최대**)
- 히어로 달 — CSS 원에서 셰이더 구체로
- 성좌 드로잉(MockChart) — SVG stroke-dashoffset에서 셰이더 라인/입자로
- 12궁 링 · 천구의 — 3D 공간에 실제로 띄우고 블룸
- 부적 카드(ArchCard) 광택 — 현행 CSS 유지가 나을 수 있음. 판단 필요

### 10.3 착수 순서 (권장)

1. **포스트프로세싱만 선행** — 그레인·비네트·블룸. 형태는 그대로인데 질감이
   올라간다. 여기서 얻는 체감이 가장 크고 위험이 가장 작다.
2. 그 결과를 보고 성운 셰이더 · 스크롤 유니폼 연동으로 확장할지 결정.
3. 전면 셰이더 전환은 마지막. 하지 않는 선택지도 유효하다.

### 10.4 제약

- §7 성능 가드레일이 그대로 적용된다. 특히 **모바일 폴백과 LCP**.
  포스트프로세싱은 픽셀 비용이라 저사양 기기에서 먼저 무너진다 — 티어별로 끈다.
- `prefers-reduced-motion`에서는 표류·블룸 애니메이션 정지.
- `/sign`·`/retrograde`·`/blog`에는 여전히 Three.js를 싣지 않는다.
- 결과 텍스트는 캔버스가 아니라 DOM으로 유지(접근성·SEO).

### 10.5 일정상 위치

**11월 동결 이후 또는 12월 신년 시즌 대응과 병행.** 색인 리드타임이 걸린
`/sign`·`/yearly`가 먼저다. 시각 품질은 트래픽을 늦추지 않는다.

---

## 11. 읽는 사람 쪽에서 본 개선 — `/today`·`/synastry`·`/yearly` **착수 전**에 처리

2026-08-12, 실제 화면(`/`, `/natal`, `/retrograde`)을 눈으로 확인하고 나온 항목이다.
남은 세 페이지를 만들기 전에 하는 이유는 단순하다 — 세 페이지가 여기서 정해질
규칙(본문 크기, 카드 내용물, 설명 방식, 정렬)을 그대로 따라 만들어질 것이기 때문이다.
나중에 고치면 네 페이지를 다 고쳐야 한다.

### 11.1 보조 설명 글자가 너무 작다

**증상.** 역행 고리 아래 `figcaption`, 천궁도 원반 아래 범례, 카드 아래 주석 같은
보조 설명이 `text-[11px]`·`text-xs`로 잡혀 있어 읽는 데 힘이 든다. 정보량이 적은
장식 문구라면 문제가 없지만, 지금 그 자리에는 **그림을 읽는 방법**이 들어 있다.
없어도 되는 글이 아니라 없으면 그림을 이해할 수 없는 글이다.

**할 일.**
- 보조 설명의 최소 크기를 `13px`(모바일) / `14px`(데스크톱)로 올린다.
  현재 `text-[11px]`이 쓰인 자리를 전수로 찾아 등급을 다시 매긴다.
- 진짜 각주(출처 표기, 시간대 안내처럼 한 번 읽고 마는 것)와 **읽는 방법**을
  구분한다. 후자는 본문과 같은 등급으로 올린다.
- 색도 함께 본다. `text-starlight-dim`(#9a96a8)이 11px에 걸리면 대비가 특히 나쁘다.
  본문 등급으로 올라오는 문장은 색도 한 단계 밝게.
- 타이포 등급표를 `docs`나 주석 한 곳에 적어 두고 새 페이지가 그것을 따르게 한다.

### 11.2 아치 카드 위쪽이 비어 있다

**이것은 새 기능이 아니라 스펙 미준수다.** 스펙 §4가 카드 내용 구성을 이미
못 박아 두었다:

> 상단 아치 안 **성좌**(금선+미색 별, 최밝성엔 이중 링) → 이름(마루부리) →
> **영문·날짜**(자간 넓은 소문자) → 선-다이아-선 → 이탤릭 한 줄 별칭

현재 상태:
- `/sign/<별자리>`의 카드 — 성좌 있음 ✅ (모프 작업 때 넣었다)
- **히어로 결과 카드**(`HeroSequence.tsx`) — `<ArchCard>`에 자식이 없어 위쪽이 통째로 빔 ❌
- **`ResultPreview`**(이런 이야기를 받게 됩니다) — 성좌 대신 빈 금색 원 하나 ❌
- 모든 카드에서 **날짜 표기 누락** — `LIBRA SUN`만 있고 `9.23 - 10.23`이 없다 ❌

**할 일.**
- `SignGlyph`를 `ArchCard`의 기본 자식으로 끌어올려, 카드를 쓰는 모든 자리에서
  성좌가 기본으로 들어가게 한다. 지금은 부르는 쪽이 매번 넣어 주게 되어 있어
  빠뜨리기 쉽다 — 실제로 두 곳에서 빠졌다.
- 라틴 표기 아래 기간(`9. 23 - 10. 23`)을 넣는다. `ZodiacSign.range`에 이미 있다.
- 최밝성 이중 링(스펙의 "최밝성엔 이중 링")도 아직 없다. 성좌마다 가장 밝은 별을
  정해 `zodiac.ts`에 표시하고 그 별만 링을 두른다.
- 카드 높이 대비 성좌 크기·여백을 다시 잡는다. 지금 비율은 성좌가 없다는 전제로
  맞춰져 있다.

### 11.3 천궁도가 친절하지 않다

**증상.** 원반의 선과 기호가 무엇을 뜻하는지 그림만 보고는 알 수 없다. 아래 본문도
"목성 궁수자리 10도 · 9하우스"처럼 점성술 어휘를 이미 아는 사람 기준으로 쓰여 있다.
지금 페이지는 **아는 사람에게는 좋고 모르는 사람에게는 벽**이다. 검색으로 들어오는
사람의 대부분은 후자다.

**할 일.**
- **원반 호버/탭 설명.** 별 기호에 커서를 올리면 "목성 — 어디서 넓어지고 무엇을
  믿는가 · 궁수자리 10도 · 9하우스(멀리 보는 방)"가 뜬다. 모바일은 탭.
  `PLANETS`의 `governs`와 `HOUSES`의 `domain`이 이미 그 문장을 갖고 있다.
- 선 종류(어스펙트 색·굵기), 바깥 띠(별자리), 안쪽 층(하우스), ASC 표식을
  **그림 옆에서 짚어 주는 범례**로 바꾼다. 지금처럼 글로만 나열하지 않고
  작은 도해를 곁들인다.
- **용어 첫 등장 자리에 한 줄 정의.** 상승궁·하우스·어스펙트·역행·오브.
  `DawnChip`처럼 눌러서 펼치는 형태가 새 창을 열지 않아 읽는 흐름을 끊지 않는다.
- 페이지 맨 위에 **"이 화면을 읽는 법" 3줄**을 둔다. 태양은 무엇, 달은 무엇,
  상승궁은 무엇 — 이 셋만 알면 나머지는 따라온다.
- 아톰 문장 자체도 검토한다. 배치 이름을 반복하는 대신 **그래서 어떻다**로
  시작하는 문장이 초심자에게 훨씬 잘 읽힌다.

### 11.4 전면 중앙 정렬과 패널 UI — 먼저 조사하고 결정한다

**질문 그대로 남겨 둔다.** 지금 거의 모든 섹션이 `text-center` + `mx-auto`다.
이 배치가 최선인지, 그리고 오버레이 패널(설정 창처럼 화면 위에 뜨는 층)을
쓸 자리가 있는지 확인이 필요하다.

**먼저 확인할 것 (구현 전).**
- awwwards 수상작에서 중앙 정렬이 실제로 어디에 쓰이는지. 관찰 가설은
  "**히어로와 전환 구간은 중앙, 읽는 구간은 좌측 정렬 + 비대칭 여백**"이다.
  긴 글을 중앙 정렬하면 줄마다 시작점이 달라져 눈이 다음 줄을 찾는 데 시간이 든다 —
  한국어처럼 줄바꿈이 잦은 언어에서 특히 그렇다. 이 가설이 맞는지 실제 사례로 확인.
- 패널/드로어 UI가 이 사이트에 맞는 자리가 있는지. 후보:
  - 천궁도 별 하나를 눌렀을 때 → 우측 패널에 그 별의 상세(§11.3의 호버 설명 확장판)
  - 용어 정의 → 작은 팝오버
  - `/synastry`의 **상대방 정보 입력** → 페이지 이동 없이 패널에서 받기
    (이 페이지를 만들기 전에 결정해야 하는 이유가 이것이다)
  - 관심사 렌즈 바꾸기 → 하단 시트
- 패널을 쓴다면 접근성 요건을 같이 정한다: 포커스 가둠, `Escape` 닫기,
  배경 스크롤 잠금, 뒤로가기로 닫히는지(모바일에서 특히), `aria-modal`.
  `Veil`의 모바일 오버레이가 이미 절반쯤 이 일을 하고 있으니 거기서 공통 부품을 뽑는다.

**결정 방식.** 시안을 말로 설명하지 말고 실제로 두 벌 만들어 붙여 놓고 고른다.
배경 mockup 때 얻은 교훈 — 조잡한 예시는 판단을 흐린다.

### 11.5 순서

1. §11.1 타이포 등급 (가장 싸고, 나머지 작업이 그 등급 위에서 진행된다)
2. §11.2 카드 내용물 (스펙 미준수 해소)
3. §11.4 정렬·패널 **조사와 결정** — `/synastry` 설계가 여기에 걸린다
4. §11.3 천궁도 친절도 (§11.4의 패널 결정을 반영해서)
5. 그다음 `/today` → `/yearly` → `/synastry`
