// 아웃라인형 골드 버튼/링크가 공유하는 색상 스타일.
// GoldButton(variant="outline")과 Veil의 네비 CTA가 함께 참조한다.
// 크기(padding/폰트)는 사용처마다 다르므로 여기 포함하지 않는다.
export const GOLD_OUTLINE_CLASSES = "border border-gold/60 text-gold-soft hover:border-gold";
