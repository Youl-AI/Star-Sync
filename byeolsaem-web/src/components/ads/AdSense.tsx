import Script from "next/script";

/**
 * 애드센스 로더.
 *
 * **사이트 전체에 깔지 않는다.** 이 스크립트를 부르는 페이지에만 광고가 붙을 수
 * 있으므로, 어디에 두느냐가 곧 광고 정책이 된다. 두 가지 이유로 페이지를 고른다.
 *
 * 1. 경험 — 메인의 수직 세계와 리딩 결과(천궁도·궁합·오늘·한 해)는 이 사이트가
 *    파는 것 자체다. 몰입 한가운데나 "우리는 지어내지 않는다"를 증명하는 자리에
 *    광고가 끼면 남는 장사가 아니다.
 * 2. 성능 — 광고 스크립트는 가볍지 않다. 메인 페이지는 하늘 셰이더 비용을 덜어내려
 *    한참을 들인 곳이라(2026-08-14), 거기에 서드파티 스크립트를 다시 얹지 않는다.
 *
 * 그래서 붙는 곳은 읽는 페이지뿐이다: 새벽 그룹(칼럼·소개·방침), 수성 역행,
 * 별자리 상세. 모두 검색으로 사람이 도착하는 자리이고 글이 길어 광고가 문맥을
 * 해치지 않는다.
 *
 * 대시보드의 자동 광고에서 전면 광고(Interstitial)는 반드시 꺼야 한다 — 페이지
 * 이동마다 전체 화면을 덮어 크로스페이드 전환과 별자리 모프를 정면으로 깨뜨린다.
 */
const PUBLISHER_ID = "ca-pub-6538739927923803";

export function AdSense() {
  return (
    <Script
      async
      src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${PUBLISHER_ID}`}
      crossOrigin="anonymous"
    />
  );
}
